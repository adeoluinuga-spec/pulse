import SurveysWorkspace from "@/components/surveys/SurveysWorkspace";

/** Anonymous staff surveys. HR only; the answering page is public and lives at /s/[slug]. */
export default function SurveysPage() {
  return <SurveysWorkspace />;
}
